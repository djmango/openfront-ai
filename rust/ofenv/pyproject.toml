[build-system]
requires = ["maturin>=1.5"]
build-backend = "maturin"

[project]
name = "ofenv"
version = "0.1.0"
description = "Native Rust RL env for openfront-ai"
requires-python = ">=3.11"

[tool.maturin]
features = ["pyo3/extension-module"]
