[build-system]
requires = ["maturin>=1.5"]
build-backend = "maturin"

[project]
name = "ofrs"
version = "0.3.0"
description = "Rust hot paths for openfront-ai (GIL-free frame decode + batch collate)"
requires-python = ">=3.11"

[tool.maturin]
features = ["pyo3/extension-module"]
